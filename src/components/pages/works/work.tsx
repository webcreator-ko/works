import React from "react";
import workStyles from "./work.module.scss";
const Work = () => {
 return (
  <section className={workStyles.wrap}>
   <h2>YOMcoma</h2>
   <div className={workStyles.inner}></div>
  </section>
 );
};

export default Work;
